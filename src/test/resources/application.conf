msbConfig {
  brokerConfig = {
    durable = false
  }
}

sap-bw-de {
  url = "http://localhost:8081"
  userName = "user"
  password = "password"
}

namespace = {
  from = "test:booking:mapping:event"
  to = "test:booking:enrich:event"
}