msbConfig {
  brokerConfig = {
    durable = false
  }
}

sap-bw-de {
  booking-url = "http://localhost:8081"
  customer-url = "http://localhost:8081"
  userName = "user"
  password = "password"
}

namespace = {
  from = "test:booking:mapping:event"
  to = "test:booking:enrich:event"
}